"""Ports consumed by application services.

- ``repositories``: domain-only persistence (entities under ``core.domain``).
- ``persistence``: technical / legacy SQL ports (tokens, event log, old cart/order lines).
"""
